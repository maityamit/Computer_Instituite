<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="growOnlearningInstituteitaberia.example.growonlearninginstitute">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrowOnLearningInstitute">
        <activity android:name=".GroupChatActivity"/>
        <activity android:name=".ChatActivity" />
        <activity android:name=".CourseDashBoardforTeachers" />
        <activity android:name=".CourseListActivityforTeachersActivity" />
        <activity android:name=".TeacherdashBoardsActivity" />
        <activity android:name=".CourseDetailsActivity" />
        <activity android:name=".ChatListActivity" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".ProfileActivity" />

        <activity android:name=".AuthtestActivity">


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.GrowOnLearningInstitute.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>